# Receiver address where users send payments
RECEIVER_ADDRESS=kaspa:youraddresshere

# Kaspa REST API base (public)
KASPA_API_BASE=https://api.kaspa.org

# Default payment checkpoint (time slice) for new sessions
CHECKPOINT_SECONDS_DEFAULT=60

# Price rate (KAS per minute). Amount per payment = rate * (checkpoint_seconds / 60) + tiny tag.
RATE_KAS_PER_MINUTE=0.1

# (Optional) How often your watcher polls is hardcoded in watcher.js for the demo.
